CREATE TABLE IF NOT EXISTS CONTACTS (ID SERIAL NOT NULL, NAME VARCHAR(255) NOT NULL, SURNAME1 VARCHAR(255) NOT NULL, SURNAME2 VARCHAR(255), PHONE INTEGER NOT NULL UNIQUE, EMAIL VARCHAR(255), 
CONSTRAINT CONTACTS_PK PRIMARY KEY (ID));